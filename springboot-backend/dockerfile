FROM eclipse-temurin:17-jdk-alpine
WORKDIR /app
COPY . .

# ✅ Fix: Give execute permissions to gradlew
RUN chmod +x gradlew

# ✅ Build your Spring Boot app
RUN ./gradlew build -x test

EXPOSE 8080
CMD ["java", "-jar", "build/libs/app.jar"]
